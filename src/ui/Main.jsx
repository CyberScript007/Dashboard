import styled from "styled-components";

const MainStyle = styled.main`
  min-height: 100vh;
  background-color: green;
`;

function Main() {
  return <MainStyle>main styled</MainStyle>;
}

export default Main;
